.carousel.categories{data: {slick: slicktrabutes}}
  - categories.each do |category|
    .carousel__category{data: {id: category.id}}
      =image_tag(category.image, width: 220)
      %h3=category.name
=link_to "Back To Categories", "#", id: "categories-back__btn", style: "display:none;"
.carousel.issues{data: {slick: slicktrabutes}}
